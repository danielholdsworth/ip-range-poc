version: 2.1

workflows:

  docker-pull-test:
    jobs:
      - pull-test

jobs:
  pull-test:
    docker:
    - image: 'cimg/base:stable'
    resource_class: small
    steps:
      - run: echo "test"
